import 'package:flutter/material.dart';
import 'package:hero_index/testing.dart';

class Edu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text(
          'Salu2 al eduardo',
          style: TextStyle(color: Colors.white, fontSize: 30),
        ),
      ),
    );
  }
}
